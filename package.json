{
  "name": "study-copilot",
  "private": true,
  "version": "0.1.0",
  "description": "Adaptive Study Planning System — root dev-runner for backend (FastAPI) + frontend (Vite) together",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"uv run uvicorn backend.app:app --reload\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
